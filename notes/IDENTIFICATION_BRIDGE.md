# Identification Bridge

Gate: `BEI_G6`
Constant: `eps_coh`

The determining class of regulator and special-value observables carries strict coherence. The gate closes exactly when the residual identification error is zero in strict mode.
